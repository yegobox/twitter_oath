import 'package:flutter/material.dart';

class CallBackPage extends StatelessWidget {
  const CallBackPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // get params do what I want to do then close the page!

    return const Text("I am callback");
  }
}
